# Legacy Workspace Instructions

This file is intentionally kept only as a compatibility pointer for older tooling.

- Repository-wide coding rules live in `.github/copilot-instructions.md`.
- Workflow, review gates, and `.ai/` memory discipline live in `AGENTS.md`.
- Path-specific guidance lives in `.github/instructions/*.instructions.md`.
- Product and business context lives in `CRM_System_Requirement.md`, `06_IMPLEMENTATION/README.md`, and `06_IMPLEMENTATION/docs/adr/`.

If this file conflicts with the `.github/` instruction set, follow the `.github/` files.
